# Devlog — 2026-08

## 2026-08-27

### Goal

Faz 0 bootstrap: repo + yönetişim + altyapı + CI, hepsi aynı gün push'lanmış şekilde.

### Done

- `git init` + public repo `yavuzcgg/akiron-commerce` + ilk push aynı gün (Temmuz veri kaybı dersi uygulandı).
- Yönetişim çekirdeği: `AGENTS.md` anayasa (~90 satır), `CLAUDE.md` (`@AGENTS.md` import), `ARCHITECTURE.md` haritası, `SYSTEM_ARCHITECTURE.md`, `ROADMAP.md`, `DATA_OWNERSHIP.md`, 2 skill (`finish-session`, `create-adr`).
- **16 ADR** (0001–0016), her biri zorunlu Exit Strategy bölümüyle.
- Build temeli: boş `.slnx` + CPM (`Directory.Packages.props` — MassTransit 8.5.10 ve MediatR 12.5.0 lisans pinleri) + `Directory.Build.props` (warnings-as-errors) + `global.json` (SDK 10.0.301).
- Altyapı compose (hepsi pinli): PostgreSQL 16 (6 DB otomatik oluştu), Redis 8, RabbitMQ 4.1, Elasticsearch 9.5.2 (512MB heap), Jaeger 2.20.0, Aspire dashboard 9.5, Mailpit v1.31. `up -d --wait` → healthcheck'li 5 servisin tamamı healthy.
- CI: build + compose-validate, ilk koşuda yeşil (17 sn).
- (Sabah, ayrı iş: VPS diski BuildKit cache prune'u ile %83 → %25, ~47GB geri geldi.)

### Learned

- Boş `.slnx` build'i NuGet'ten "restore edilecek proje yok" uyarısı basıyor — exit 0, TreatWarningsAsErrors proje-seviyesi olduğu için CI'ı kırmıyor; Faz 1'de ilk proje gelince kaybolacak.
- Jaeger v2 ve Aspire dashboard'un ikisi de OTLP 4317/4318 istiyor — çakışmayı Aspire'ı host 4319'a map'leyerek çözdük; servisler birincil hedef olarak Jaeger'a basacak.
- MassTransit son 8.x = 8.5.10; v8 EOL 2026 sonu (ADR-0005'te karar kapısı Faz 5 başına kuruldu).

### Decisions

- ADR-0001…0016 toplu kaydedildi (mimari, iletişim kuralı, lisans pinleri, Marten, ES, Redis lisansı, YARP, auth, bayi fiyatlama, frontend, deploy topolojisi, Aspire, isimlendirme).

### Problems

- Yok — bootstrap temiz geçti.

### Next

- Faz 1: Catalog dikey dilimi — domain (price group + markup zinciri) → EF Core + ilk migration → endpoint modülleri → Testcontainers → Dockerfile → path-filtered CI → Jaeger'da ilk trace.
- Faz 1 ile doğacak dosyalar: `Catalog/AGENTS.md`, `.claude/rules/dotnet.md` + `testing.md`, `DOMAIN_GLOSSARY.md`, `API_CONVENTIONS.md`, ilk feature planı (`docs/plans/active/001-…`), `runbooks/local-development.md`.

### Commits

`2482645` chore: bootstrap solution · `c6eed95` chore: .gitattributes · `0bfca90` docs: governance core · `d50c3a0` docs: ADR batch · `8e592d7` infra: compose · `86e9bb3` ci: pipeline · (bu dosya) docs: devlog + roadmap check-off
